tmp/

# Local clones of other people's configs, kept for reference only.
.references/

# Claude agent memory is runtime state: keep the dir, not its contents.
nix/home/claude/agent-memory/*
!nix/home/claude/agent-memory/.gitkeep

# This file is normally created via `touch`, and not symlinked from an existing file in the repo, but just in case... ⚠
.gitconfig.local
